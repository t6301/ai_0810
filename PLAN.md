# 時事命題網站開發計畫

## 執行規則

- 開發順序固定為 M0 → M1 → M2 → M3 → M4 → M5。
- 一次只做一個里程碑，不順手提前製作下一關功能。
- 每一關動工前先說明計畫、修改檔案與風險，經大師同意後才開始。
- 每一關完成後由大師親自驗收；大師回覆「通過」後才建立 Git 版本紀錄。
- 未經大師驗收通過，不得開始下一個里程碑。
- 全程遵守 `AGENTS.md`、`SPEC.md` 與安全紅線。

## 里程碑總覽

| 里程碑 | 主題 | 狀態 |
| --- | --- | --- |
| M0 | 網站骨架 | 已完成並驗收 |
| M1 | 表單與本機保存 | 尚未開始 |
| M2 | AI 出題 | 尚未開始 |
| M3 | Google 登入 | 尚未開始 |
| M4 | 雲端紀錄 | 尚未開始 |
| M5 | 正式部署 | 尚未開始 |

## M0｜網站骨架

### 要做的事

1. 建立可直接雙擊開啟的 `index.html`，並連接純 CSS 與 JavaScript。
2. 完成新聞素材、出題設定、草稿區與查核提醒的基本畫面。
3. 支援電腦、手機及 A4 列印版面，未完成功能不顯示成可操作按鈕。

### 不做的事

- 不連接 AI、不建立登入、不保存資料、不上傳雲端。
- 不產生假題目，也不假裝列印或下載功能已完成。

### 驗收清單

- [x] 雙擊 `index.html` 可以正常開啟。
- [x] 可以切換「貼新聞網址」與「貼新聞文字」。
- [x] 漏填素材、媒體名稱、日期或難度時，會顯示繁體中文提醒。
- [x] 填妥資料後會顯示「素材已準備完成」。
- [x] 尚未完成的列印與下載按鈕不會出現在畫面上。
- [x] JavaScript 語法與檔案連結檢查通過。

## M1｜表單與本機保存

### 要做的事

1. 完成網址、新聞文字、媒體名稱、日期與難度的輸入及白話檢查。
2. 將老師尚未送出的素材與設定保存在目前瀏覽器，重新開啟後可以繼續。
3. 提供本機草稿狀態與清除目前草稿的明確操作，清除前必須再次確認。

### 不做的事

- 不連接 AI、不登入 Google、不把資料送到雲端。
- 不保存或要求任何學生個人資料。
- 不製作真正的題目列印與下載功能。

### 驗收清單

- [ ] 網址與文字兩種輸入方式都能正常切換並保留各自內容。
- [ ] 素材不完整或日期缺漏時，不會進入下一步。
- [ ] 關閉瀏覽器再重新開啟，未完成草稿仍然存在。
- [ ] 清除草稿前會先詢問，取消後資料不會消失。
- [ ] 換一個瀏覽器時不會看到原本的本機草稿。
- [ ] 手機畫面與 A4 預覽不會左右超出。

## M2｜AI 出題

### 要做的事

1. 由 `api/` 底下的程式安全呼叫 AI，產生 4 題四選一單選題與 1 題簡答題。
2. 顯示可由老師修改的「草稿」，每題附媒體名稱、日期、答案與詳解。
3. 完成分開列印與下載「學生題目卷」及「老師解答卷」。

### 不做的事

- 不把 API 金鑰放進 HTML、前端 JavaScript、Git 或對話紀錄。
- 不替老師勾選「已查核」，也不自動判定題目可直接使用。
- 不做 Google 登入或雲端出題紀錄。

### 驗收清單

- [ ] 使用一則完整素材，可依所選難度產生 5 題。
- [ ] 單選題都是四選一，且只有一個正確答案。
- [ ] 每題都標示「草稿」，並附出處提醒、答案與詳解。
- [ ] 老師可以修改題幹、選項、答案與詳解。
- [ ] AI 失敗時會保留素材與設定，並顯示白話提醒及再次嘗試按鈕。
- [ ] 學生題目卷不顯示答案，老師解答卷顯示答案與詳解。
- [ ] 兩份文件都能列印成 A4 並下載。

## M3｜Google 登入

### 要做的事

1. 提供老師使用 Google 帳號登入及登出。
2. 未登入者只能看到入口說明，登入後才可使用命題功能。
3. 登入身分由 `api/` 底下的程式安全確認，前端不保存登入祕密。

### 不做的事

- 不建立學生登入、不自建密碼系統、不保存 Google 密碼。
- 不在這一關建立跨電腦的雲端出題紀錄。
- 不增加其他登入方式。

### 驗收清單

- [ ] 未登入時無法進入命題畫面或查看題目。
- [ ] 老師可以用 Google 帳號登入，畫面會顯示目前登入身分。
- [ ] 老師可以登出，登出後個人內容立即隱藏。
- [ ] 重新整理頁面後，登入狀態符合安全設定。
- [ ] 登入失敗或取消時會顯示繁體中文提醒，不會清除本機草稿。
- [ ] 專案檔案與 Git 紀錄中沒有密碼或登入祕密。

## M4｜雲端紀錄

### 要做的事

1. 保存老師自己的題目、素材來源、日期、難度與修改結果。
2. 提供個人出題紀錄清單，讓老師開啟並繼續修改自己的草稿。
3. 讓同一位老師換電腦登入後仍能查看紀錄，並隔離不同老師的資料。

### 不做的事

- 不保存學生個人資料、不開放學生查看答案。
- 不讓老師查看其他老師的紀錄，也不製作分享功能。
- 不做自動批改、成績統計或班級管理。

### 驗收清單

- [ ] 老師保存題目後，關閉網站再登入仍能找到紀錄。
- [ ] 同一帳號換一台電腦登入，仍能看到自己的紀錄。
- [ ] 老師可以從紀錄清單開啟、修改並再次保存草稿。
- [ ] 使用另一個測試帳號時，看不到前一個帳號的紀錄。
- [ ] 儲存失敗時保留畫面內容，並提醒老師先下載備份。
- [ ] 雲端只保存老師帳號與老師自己的出題紀錄。

## M5｜正式部署

### 要做的事

1. 將網站部署到 Vercel，設定正式網址與必要的環境變數。
2. 以電腦與手機完成登入、出題、修改、保存、列印及下載的完整驗收。
3. 檢查公開檔案、錯誤訊息、資料隔離與金鑰保護，留下可安全回復的版本。

### 不做的事

- 不在部署時臨時增加新功能或重排已驗收畫面。
- 不公開 API 金鑰、環境變數、老師紀錄或任何憑證。
- 不略過失敗情況、手機版與 A4 列印檢查。

### 驗收清單

- [ ] 正式網址可由電腦與手機正常開啟，畫面不會左右超出。
- [ ] Google 登入、AI 出題與個人雲端紀錄都能正常使用。
- [ ] 學生題目卷與老師解答卷可以分開列印及下載。
- [ ] 網址讀不到、AI 失敗、登入失敗及儲存失敗時都有白話提醒。
- [ ] 未登入者、學生及其他老師看不到不屬於自己的題目或答案。
- [ ] GitHub 與瀏覽器中找不到 API 金鑰、密碼或憑證。
- [ ] 大師可以依照驗收步驟從頭到尾親自操作一次。
