# 專案工作規則（每次開工前先讀這一份）

## 這是什麼專案

給台灣高中職老師自己用的「時事命題網站」。
使用者是老師，不是工程師。所有解釋都要讓一個不會寫程式的人看得懂。

## 你要怎麼跟我說話

- 全程繁體中文、台灣用語。
- 不要用我沒學過的英文術語；非用不可時，先用一句白話解釋。
- 結論先講，再講理由。不要長篇大論。
- 不確定的時候先問我，不要自己猜。有多種做法就列出來讓我選。

## 技術限制（不可違反）

- 只用純 HTML / CSS / JavaScript，不要引入任何框架或套件。
- index.html 必須可以直接雙擊、用瀏覽器打開就會動。
- 介面文字、錯誤訊息、按鈕全部繁體中文。
- 版面在電腦和手機都不能橫向溢位，而且要能列印成 A4。

## 安全紅線（違反等於整個專案報廢）

- API 金鑰只能存在 Vercel 的環境變數，只能由 api/ 底下的程式讀取。
- 絕對不可以把任何金鑰寫進 HTML、前端 JavaScript，或任何會被存進 git 的檔案。
- 一律建立並維護 .gitignore，排除 .env 與任何憑證檔。
- 我如果不小心把金鑰貼在對話裡，請立刻提醒我，而且不要把它寫進任何檔案。
- 不蒐集、不儲存、不傳送任何學生個人資料。
  資料庫裡只存「老師本人的帳號」與「老師自己的出題紀錄」。

## 內容原則

- 新聞素材一律改寫成自足的情境題幹，不逐字照抄原文。
- 每一題都要附出處提醒（媒體名稱＋日期）與詳解。
- AI 產出的題目一律標示為「草稿」。你不可以代替我勾選任何「已查核」欄位。
- 題目的最終決定權在我。請主動提醒我查核：事實、時效、著作權、偏誤、答案唯一性。

## 工作節奏（每一關都一樣）

1. 一次只做一個里程碑，即使你覺得順手可以多做也不要做。
2. 動工前先用白話講計畫、會改哪些檔案、可能弄壞什麼，然後停下來問我「可以開工嗎」。
   我說可以，你才動手。
3. 一次不要改超過三個檔案。
4. 做完要告訴我改了什麼、我要點哪裡才能親自驗收。不要只說「已完成」。
5. 我說「通過」之後才幫我存檔（git commit），訊息用繁體中文寫清楚這一關做了什麼。
6. 不要刪掉我既有的內容，除非我明講。

## 不要做的事

- 不要為了「以後好擴充」增加我沒要求的功能、設定或選項。
- 不要順手美化、重排跟這次需求無關的程式碼。
- 不要在我沒同意前執行會連網、會安裝東西、會刪除檔案的指令。
- 不要建議我用系統管理員身分執行或關閉任何安全設定。
  真的需要管理員權限，就直接告訴我，讓我自己決定。
- 尚未完成的功能不可顯示成可操作按鈕；應隱藏或清楚說明啟用條件，避免老師誤以為故障。

## 這份規則會長大

如果你搞錯了某件事而我糾正你，請主動把正確的規則補寫進這個檔案，讓下次不會再犯。
補寫前先把你要加的那幾行念給我聽。
